#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-push checks (build + API typecheck)..."

echo "📦 Building frontend..."
npm run build || exit 1

echo "📝 Type-checking API (Workers)..."
npm run typecheck:api || exit 1

echo "✅ Pre-push checks passed!"
