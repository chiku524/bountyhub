#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

echo "🔍 Running linter..."
npm run lint 2>&1 | grep -qE "(error|✖)" && exit 1 || true

echo "✅ Pre-commit passed (lint). Run 'npm run typecheck:all' before push or rely on CI."
