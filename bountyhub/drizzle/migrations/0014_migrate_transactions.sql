-- Migration to help with transaction table consolidation
-- This migration doesn't drop the old table yet, but prepares for it

-- Add a comment to document the migration process
-- Note: After running the migration script via the API, you can safely drop the wallet_transactions table 